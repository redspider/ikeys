<!DOCTYPE html>
<html>
    <head>
        <title>OSX Remote panel</title>
        <link rel="stylesheet" href="/static/css/app.css" />
        <script src="http://code.jquery.com/jquery-1.8.3.min.js"></script>
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <script>
            $(function () {
                $('.status').text("Ready");

                $('.btn').bind('touchstart', function(e) {
                        e.preventDefault();
                        $('.status').text($(this).data('trigger'));
                        $.post('/trigger',{'trigger': $(this).data('trigger'), 'time': new Date()}, function (data) {$('.status').text(data); });
                        $('.status').text("Done");
                    }
                );
            });
        </script>
    </head>
    <body>
        <div class="container-fluid">
            <div class="row-fluid">
                <div class="span12">
                    <a class="btn btn-large" data-trigger="bell" href="#">Bell</a>
                    <a class="btn btn-large" data-trigger="expose" href="#">Exposé</a>
                    <a class="btn btn-large" data-trigger="chrome" href="#">Chrome</a>
                    <a class="btn btn-large" data-trigger="pycharm" href="#">PyCharm</a>
                    <a class="btn btn-large" data-trigger="textual" href="#">Textual</a>
                    <a class="btn btn-large" data-trigger="sparrow" href="#">Sparrow</a>
                    <a class="btn btn-large" data-trigger="mute" href="#">Mute</a>
                    <a class="btn btn-large btn-danger" data-trigger="lock" href="#">Lock screen</a>
                    <div class="status"></div>
                </div>
            </div>
        </div>
    </body>
</html>